import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  wrapper: {alignItems: 'center', justifyContent: 'center'},
  seletitle: {fontSize: 20, color: '#fff'},
  title: {fontSize: 16, color: '#fff'},
});

export default style;
