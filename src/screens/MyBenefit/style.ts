import { StyleSheet } from 'react-native'
import { wrapperBg } from '../../config/style.config'

const style = StyleSheet.create({
  wrapper: { flex: 1, backgroundColor: wrapperBg },
})

export default style
