import { StyleSheet } from 'react-native'

const style = StyleSheet.create({
  list: {
    flexWrap: 'wrap',
    flexDirection: 'row',
    marginHorizontal: 16,
    backgroundColor: '#fff',
    borderRadius: 5,
  },
})

export default style
