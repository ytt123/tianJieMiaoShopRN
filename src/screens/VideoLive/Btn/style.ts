import { Dimensions, StyleSheet } from 'react-native'

export default StyleSheet.create({
  btnWrapper: {
    position: 'absolute',
    right: 20,
    bottom: 100,
  },

  icon: {
    height: 35,
    width: 35,
    marginBottom: 10,
  },
})
