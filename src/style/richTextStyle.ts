import { StyleSheet } from 'react-native'

/**
 * 富文本
 */
export const richTextStyle = StyleSheet.create({
  p: {
    lineHeight: 16,
  },
  img: {
    fontSize: 0,
    maxWidth: '100%',
  },
  em: {
    fontStyle: 'italic',
  },
})
